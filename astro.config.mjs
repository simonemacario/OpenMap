import { defineConfig } from "astro/config";

// Static output (default). Set `site` to your deploy URL for correct canonical/sitemap.
// For GitHub Pages at https://simonemacario.github.io/OpenMap set base: "/OpenMap".
export default defineConfig({
  site: "https://simonemacario.github.io",
  base: "/OpenMap",
});
