{
  "name": "openmap-malaysia",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "OpenMap — Market Intelligence on Italian business in Malaysia. A continuously updated company database with a market-stats homepage.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:data": "node scripts/check-data.mjs",
    "test": "node scripts/check-data.mjs && node --test"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
